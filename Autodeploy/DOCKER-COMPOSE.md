# Docker Compose - Полное руководство

## 📦 Архитектура приложения

Приложение состоит из двух сервисов:

1. **Backend (Flask)** - API на Python Flask
   - Порт: 5000
   - Эндпоинты: `/`, `/health`, `/info`, `/echo/<message>`, `/multiply/<a>/<b>`, `/divide/<a>/<b>`, `/subtract/<a>/<b>`

2. **Frontend (Nginx + HTML/CSS/JS)** - Веб-интерфейс
   - Порт: 80
   - Проксирует API запросы к бэкенду
   - Красивый интерактивный dashboard

## 🚀 Быстрый старт

### 1. Запуск всего приложения

```bash
docker-compose up -d
```

Эта команда:
- Соберет образы для backend и frontend
- Создаст сеть для взаимодействия сервисов
- Запустит контейнеры в фоновом режиме

### 2. Открыть приложение

Откройте браузер и перейдите по адресу:
```
http://localhost
```

Вы увидите интерактивный dashboard с:
- Информацией о сервисе
- Данными о контейнере
- Echo тестом
- Калькулятором
- Health check статусом

## 📋 Основные команды Docker Compose

### Управление сервисами

```bash
# Запуск всех сервисов
docker-compose up

# Запуск в фоновом режиме
docker-compose up -d

# Запуск с пересборкой образов
docker-compose up --build

# Остановка всех сервисов
docker-compose down

# Остановка с удалением volumes
docker-compose down -v

# Перезапуск всех сервисов
docker-compose restart

# Перезапуск конкретного сервиса
docker-compose restart backend
docker-compose restart frontend
```

### Просмотр состояния

```bash
# Список запущенных сервисов
docker-compose ps

# Логи всех сервисов
docker-compose logs

# Логи конкретного сервиса
docker-compose logs backend
docker-compose logs frontend

# Логи в реальном времени
docker-compose logs -f

# Последние 100 строк логов
docker-compose logs --tail=100
```

### Работа с отдельными сервисами

```bash
# Запуск только backend
docker-compose up backend

# Остановка только frontend
docker-compose stop frontend

# Пересборка конкретного сервиса
docker-compose build backend

# Масштабирование (только для stateless сервисов)
docker-compose up -d --scale backend=3
```

### Выполнение команд в контейнерах

```bash
# Открыть bash в backend контейнере
docker-compose exec backend /bin/bash

# Открыть sh в frontend контейнере
docker-compose exec frontend /bin/sh

# Выполнить команду без входа в контейнер
docker-compose exec backend python --version
```

## 🔍 Тестирование API

### Через веб-интерфейс
Откройте `http://localhost` в браузере и используйте интерактивный dashboard.

### Через curl (прямые запросы к backend)

```bash
# Главная страница
curl http://localhost:5000/

# Health check
curl http://localhost:5000/health

# Информация о контейнере
curl http://localhost:5000/info

# Echo тест
curl http://localhost:5000/echo/Hello

# Умножение
curl http://localhost:5000/multiply/5/3

# Деление
curl http://localhost:5000/divide/10/2

# Вычитание
curl http://localhost:5000/subtract/10/3
```

### Через фронтенд API proxy

```bash
# Все запросы можно делать через /api/
curl http://localhost/api/health
curl http://localhost/api/info
curl http://localhost/api/echo/Test
```

## 🌐 Сетевое взаимодействие

### Внутренняя сеть Docker

Docker Compose создает сеть `app-network`, в которой сервисы могут общаться по именам:

- Frontend обращается к backend: `http://backend:5000`
- Backend доступен внутри сети как: `backend`
- Frontend доступен внутри сети как: `frontend`

### Порты

- **Frontend**: `localhost:80` → контейнер `80`
- **Backend**: `localhost:5000` → контейнер `5000`

## 🛠️ Разработка и отладка

### Режим разработки с auto-reload

Для разработки backend с автоперезагрузкой, измените `docker-compose.yml`:

```yaml
services:
  backend:
    # ... остальное
    volumes:
      - ./app.py:/app/app.py
    environment:
      - ENV=development
      - FLASK_DEBUG=1
```

### Просмотр использования ресурсов

```bash
docker-compose stats
```

### Инспекция конфигурации

```bash
# Проверить конфигурацию
docker-compose config

# Проверить конфигурацию с подстановкой переменных
docker-compose config --resolve-image-digests
```

## 🔧 Устранение неполадок

### Проблема: Порт уже занят

```bash
# Изменить порты в docker-compose.yml
services:
  frontend:
    ports:
      - "8080:80"  # Вместо 80:80
  backend:
    ports:
      - "5001:5000"  # Вместо 5000:5000
```

### Проблема: Сервис не запускается

```bash
# Посмотреть логи
docker-compose logs backend

# Пересобрать с нуля
docker-compose down
docker-compose build --no-cache
docker-compose up
```

### Проблема: Frontend не может подключиться к Backend

1. Проверьте, что оба сервиса запущены:
```bash
docker-compose ps
```

2. Проверьте логи nginx:
```bash
docker-compose logs frontend
```

3. Проверьте сеть:
```bash
docker network inspect autodeploy_app-network
```

### Проблема: Изменения в коде не применяются

```bash
# Пересоберите образы
docker-compose down
docker-compose build --no-cache
docker-compose up -d
```

## 🧹 Очистка

### Удаление всех контейнеров и сетей

```bash
docker-compose down
```

### Удаление с volumes

```bash
docker-compose down -v
```

### Полная очистка (включая образы)

```bash
docker-compose down --rmi all -v
```

### Очистка неиспользуемых Docker ресурсов

```bash
docker system prune -a
```

## 📊 Мониторинг

### Health Check

Backend имеет встроенный health check:
```bash
docker-compose ps
```

Статус должен быть `healthy`.

### Просмотр health check логов

```bash
docker inspect flask-backend | grep -A 10 Health
```

## 🔐 Production настройки

Для production окружения рекомендуется:

1. **Использовать production WSGI сервер** (вместо Flask dev server)
2. **Настроить HTTPS** через Let's Encrypt
3. **Добавить ограничение ресурсов**:

```yaml
services:
  backend:
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M
        reservations:
          memory: 256M
```

4. **Использовать docker secrets** для чувствительных данных
5. **Настроить логирование** в внешний сервис

## 📁 Структура проекта

```
.
├── docker-compose.yml          # Конфигурация compose
├── Dockerfile                  # Dockerfile для backend
├── app.py                      # Flask приложение
├── requirements.txt            # Python зависимости
├── frontend/                   # Директория frontend
│   ├── Dockerfile             # Dockerfile для frontend
│   ├── nginx.conf             # Конфигурация nginx
│   ├── index.html             # Главная страница
│   ├── style.css              # Стили
│   └── app.js                 # JavaScript логика
├── README.md                   # Основная документация
└── DOCKER-COMPOSE.md          # Документация Docker Compose
```

## 🎯 Полезные советы

1. **Используйте `.env` файл** для переменных окружения
2. **Именуйте контейнеры** для удобства (уже сделано)
3. **Используйте `depends_on`** для контроля порядка запуска
4. **Настройте restart policies** для автоматического перезапуска
5. **Используйте volumes** для персистентности данных

## 🚀 Следующие шаги

- Добавьте базу данных (PostgreSQL, MongoDB)
- Настройте CI/CD pipeline
- Добавьте Redis для кеширования
- Реализуйте аутентификацию
- Настройте мониторинг (Prometheus, Grafana)

