# Тестовое Docker приложение на Python

Полноценное Flask веб-приложение с красивым веб-интерфейсом для тестирования Docker контейнеров.

## ⚡ Быстрый старт

```bash
# Запустить приложение
docker-compose up -d

# Открыть в браузере
# http://localhost
```

📖 **Подробная инструкция:** [QUICKSTART.md](QUICKSTART.md)

## 🎯 Возможности

- ✅ **Flask REST API** - полноценный бэкенд
- 🌐 **Веб-интерфейс** - интерактивный dashboard
- 🐳 **Docker Compose** - оркестрация сервисов
- 🔄 **Health Checks** - мониторинг состояния
- 🔢 **Калькулятор API** - умножение, деление, вычитание
- 💬 **Echo сервис** - тестирование API
- 📊 **Информация о контейнере** - hostname, версия Python

## 📁 Структура проекта

```
.
├── docker-compose.yml          # Оркестрация сервисов
├── Dockerfile                  # Backend образ
├── app.py                      # Flask приложение
├── requirements.txt            # Python зависимости
├── frontend/                   # Frontend приложение
│   ├── Dockerfile             # Frontend образ (Nginx)
│   ├── nginx.conf             # Конфигурация Nginx
│   ├── index.html             # Веб-интерфейс
│   ├── style.css              # Стили
│   └── app.js                 # JavaScript
├── README.md                   # Основная документация
├── QUICKSTART.md              # Быстрый старт
└── DOCKER-COMPOSE.md          # Docker Compose руководство
```

## 🏗️ Архитектура

```
┌──────────────────────────────────┐
│     Браузер (localhost)          │
└────────────┬─────────────────────┘
             │
             ↓
┌────────────────────────────────────┐
│  Frontend (Nginx) - Port 80        │
│  • Статические файлы               │
│  • Reverse proxy для API           │
└────────────┬───────────────────────┘
             │ /api/* → backend:5000
             ↓
┌────────────────────────────────────┐
│  Backend (Flask) - Port 5000       │
│  • REST API                        │
│  • Бизнес-логика                   │
└────────────────────────────────────┘
```

## Требования

- Docker установлен на вашей системе
- Порт 5000 свободен (или используйте другой порт)

## Сборка Docker образа

### 1. Сборка образа

```bash
docker build -t test-python-app .
```

Где:
- `-t test-python-app` — имя образа (можно использовать любое)
- `.` — путь к директории с Dockerfile

### 2. Просмотр созданных образов

```bash
docker images
```

Вы должны увидеть образ `test-python-app` в списке.

## Запуск контейнера

### Базовый запуск

```bash
docker run -p 5000:5000 test-python-app
```

### Запуск в фоновом режиме

```bash
docker run -d -p 5000:5000 --name my-test-app test-python-app
```

Где:
- `-d` — запуск в фоновом режиме (detached)
- `-p 5000:5000` — проброс портов (хост:контейнер)
- `--name my-test-app` — имя контейнера

### Запуск с переменными окружения

```bash
docker run -d -p 5000:5000 -e ENV=staging --name my-test-app test-python-app
```

## Тестирование приложения

После запуска контейнера, приложение доступно по адресу `http://localhost:5000`

### Доступные эндпоинты:

1. **Главная страница**
   ```bash
   curl http://localhost:5000/
   ```

2. **Проверка здоровья**
   ```bash
   curl http://localhost:5000/health
   ```

3. **Информация о контейнере**
   ```bash
   curl http://localhost:5000/info
   ```

4. **Эхо сообщения**
   ```bash
   curl http://localhost:5000/echo/hello
   ```

## Управление контейнером

### Просмотр запущенных контейнеров

```bash
docker ps
```

### Просмотр всех контейнеров (включая остановленные)

```bash
docker ps -a
```

### Просмотр логов контейнера

```bash
docker logs my-test-app
```

### Просмотр логов в реальном времени

```bash
docker logs -f my-test-app
```

### Остановка контейнера

```bash
docker stop my-test-app
```

### Запуск остановленного контейнера

```bash
docker start my-test-app
```

### Перезапуск контейнера

```bash
docker restart my-test-app
```

### Удаление контейнера

```bash
docker rm my-test-app
```

### Удаление контейнера с принудительной остановкой

```bash
docker rm -f my-test-app
```

## Удаление образа

```bash
docker rmi test-python-app
```

## Полезные команды

### Вход в контейнер (для отладки)

```bash
docker exec -it my-test-app /bin/bash
```

### Просмотр использования ресурсов

```bash
docker stats my-test-app
```

### Инспекция контейнера

```bash
docker inspect my-test-app
```

## Очистка Docker

### Удаление всех остановленных контейнеров

```bash
docker container prune
```

### Удаление неиспользуемых образов

```bash
docker image prune
```

### Полная очистка системы

```bash
docker system prune -a
```

## Docker Compose

Для запуска полного приложения с веб-интерфейсом используйте Docker Compose:

```bash
# Запуск всего приложения (backend + frontend)
docker-compose up -d

# Открыть в браузере
# http://localhost
```

Подробная документация по Docker Compose: **[DOCKER-COMPOSE.md](DOCKER-COMPOSE.md)**

### Быстрые команды Compose

```bash
# Остановка
docker-compose down

# Просмотр логов
docker-compose logs -f

# Перезапуск
docker-compose restart

# Пересборка и запуск
docker-compose up --build
```

## Устранение неполадок

### Порт уже используется

Если порт 5000 занят, используйте другой порт на хосте:

```bash
docker run -d -p 8080:5000 --name my-test-app test-python-app
```

Приложение будет доступно по адресу `http://localhost:8080`

### Проблемы с сетью

Проверьте, что контейнер запущен и слушает правильный порт:

```bash
docker logs my-test-app
```

## Автор

Тестовое приложение для изучения Docker

